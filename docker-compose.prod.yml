# Production-oriented compose override.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# Differences from base compose:
# - PostgreSQL is not exposed on the host (internal network only)

services:
  postgres:
    ports: []
